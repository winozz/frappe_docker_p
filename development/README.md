# Development Workspace

Organized folders:
- `scripts/` – utility scripts (seeders, backups, chart fixes).
- `backups/` – exported JSON backups.
- `logs/` – collected logs moved out of the root.

Run scripts example:
```sh
python development/scripts/seed_number_cards_sample.py
```
